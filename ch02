-- (모든 명령어 앞에) docker container

ls - 실행중인 컨테이너 확인

ls --all 종료된 컨테이너도 확인

top (컨테이너 명)실행중 인 컨테이너에서 실행중인 프로세스 확인

logs 대상 컨테이너에서 모든 로그 수집

inspect  json형태로 컨테이너의 상세 정보 확

run (실행) --detach(백그라운드에서 실행 javac) --publish(호스팅된 컴퓨터에 공개)

stats 자원 할당 보기

rm 삭제 --froce(강제종료)
  
